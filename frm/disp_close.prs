[Main]
SQLStr=select id, name_r from s_brig where char_length(prava)>10 order by name_r
MainSQlStr=and z.id_disout  in (select id from s_brig  where #)
n_field=id
DbName=DBN_AVAR
L_field=name_r
Text=Диспетчер, що закрив наряд



